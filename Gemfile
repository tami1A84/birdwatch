source "https://rubygems.org"

# nostrd: relay transport (WsTransport v2 is IO.select based, no EventMachine)
gem "websocket", "~> 1.2"

# nostrd: event cache / relays / person_relays (WAL mode)
gem "sqlite3", "~> 2.0"

# nostr-tui: terminal rendering
gem "curses", "~> 1.4"
